{
  "name": "smc-pro-scanner",
  "version": "1.0.0",
  "description": "Client-side Smart Money Concept signal scanner (Vite + TypeScript, no backend).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
